#The text below is for activating UFW

	echo "Now we gonna activate and configure UFW"
		ufw enable

		ufw default deny incoming

			ufw allow http
			ufw allow ssh

		ufw default allow outgoing

		ufw status numbered

	echo "Now ufw is configured and enabled on start up"
